# Teletype to EPUB Converter

Быстрая утилита для скачивания глав с [Teletype.in](https://teletype.in) и сборки их в электронную книгу формата EPUB. 

Теперь работает **без браузера** (использует прямые HTTP-запросы), что делает сборку книг моментальной и легкой для системы.

## ✨ Возможности

- 🚀 **Высокая скорость**: Использует `requests` вместо медленного Selenium. Не требует открытия браузера.
- 📱 **Мобильность**: Может работать даже на серверах без графического интерфейса.
- 📋 **Интерактивное меню**: Удобный выбор настроек через консоль.
- 🔄 **Умный парсинг**: Поддержка нескольких источников (редакторов/авторов) для одной главы.
- ⚖️ **Система приоритетов**: Вы выбираете, чью версию главы скачивать в первую очередь.
- 🖼 **Картинки**: Автоматическое скачивание и вставка изображений (опционально).
- 💾 **Кэширование**: Повторный запуск не скачивает уже загруженные главы.
- 🎨 **Кастомизация**: Поддержка добавления обложки.

## 🛠 Требования

- Python 3.8+
- Интернет-соединение

Для работы **НЕ НУЖЕН** Google Chrome или ChromeDriver.

## 📦 Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ВАШ_АККАУНТ/teletype-epub.git
   cd teletype-epub
   ```

2. **Создайте виртуальное окружение (рекомендуется):**
   *Linux/macOS:*
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```
   *Windows:*
   ```powershell
   python -m venv venv
   venv\Scripts\activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

## 🚀 Использование

### 1. Подготовка ссылок
Создайте файл `links.txt` (или используйте `example.txt`). Формат может быть произвольным, скрипт ищет паттерн ссылок Teletype:
```text
Глава 1 (https://teletype.in/@author/post-1)
Глава 2 (https://teletype.in/@author/post-2)
...
```
*Скрипт автоматически распознает номер главы и автора из ссылки.*

### 2. Запуск
```bash
python3 build_epub.py
```

### 3. Следуйте инструкциям на экране
1. Введите название и автора книги.
2. Укажите файл со ссылками.
3. Выберите приоритет авторов (если ссылок на одну главу несколько).
4. Укажите диапазон глав (например, с 1 по 100).
5. Настройте скачивание картинок и обложку.

EPUB файл будет создан в текущей папке.

## 🔍 Как это работает?
Teletype — это SPA (Single Page Application). Большинство парсеров не видят текст, так как он рендерится через JavaScript.
Этот скрипт извлекает данные напрямую из Hydration (JSON) состояния страницы, что позволяет получить "сырой" и чистый контент без запуска тяжелого браузерного движка.
